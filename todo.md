# PMS TODO

이 문서는 현재 개발상황 기준으로 앞으로 진행할 작업 우선순위를 정리한 로드맵입니다.

## 0. 현재 상태 요약

- 백엔드: FastAPI 기반 API 서버
- 프론트엔드: React + Vite 기반 UI
- 인증: FastAPI 자체 JWT 로그인 방식
- 주요 구현:
  - 프로젝트 목록 조회
  - 태스크 목록 조회
  - 태스크 생성
  - 태스크 수정/삭제 API
  - FastAPI JWT 로그인/회원가입 화면
  - JWT 기반 현재 유저 복구
  - Asana 스타일 칸반 UI

## 1. 최우선 과제: JWT 인증 방향 결정

교수님 조언에 따라 인증 구조를 JWT 기반으로 정리해야 합니다.

결정: `선택지 A. FastAPI 자체 JWT 로그인`으로 진행합니다.

### 선택지 A. FastAPI 자체 JWT 로그인

- 백엔드가 회원가입/로그인을 직접 처리합니다.
- 로그인 성공 시 백엔드가 JWT access token을 발급합니다.
- 프론트는 API 요청마다 `Authorization: Bearer <token>` 헤더를 보냅니다.
- 백엔드는 JWT를 검증해서 현재 유저를 식별합니다.

장점:

- 과제 설명에 가장 적합합니다.
- JWT 발급/검증 흐름을 직접 구현했다는 점이 명확합니다.
- Firebase 의존도를 줄일 수 있습니다.

단점:

- 현재 Firebase 로그인 코드를 일부 갈아엎어야 합니다.
- 비밀번호 해싱, 토큰 만료, 로그인 API 등을 직접 구현해야 합니다.

### 선택지 B. Firebase ID Token 검증

- 프론트 로그인은 Firebase를 유지합니다.
- 프론트가 Firebase ID Token을 백엔드 API 요청에 보냅니다.
- 백엔드는 Firebase Admin SDK로 토큰을 검증합니다.

장점:

- 현재 구조를 덜 바꿉니다.
- Firebase 로그인 기능을 계속 활용할 수 있습니다.

단점:

- JWT 기반이긴 하지만, 백엔드가 직접 JWT를 발급하는 구조는 아닙니다.
- 과제에서 “직접 구현한 JWT 인증”을 기대한다면 설명력이 약할 수 있습니다.

### 권장 방향

- 과제/교수님 피드백 대응을 우선하면 `선택지 A. FastAPI 자체 JWT 로그인`을 권장합니다.
- 기존 Firebase 기능을 최대한 살리고 싶다면 `선택지 B. Firebase ID Token 검증`을 선택합니다.

## 2. JWT 인증 구현 TODO

- [x] 인증 방향 결정: 자체 JWT 방식 또는 Firebase ID Token 검증 방식
- [x] 백엔드 유저 모델에 비밀번호 해시 필드 필요 여부 확인
- [x] 회원가입 API 설계
- [x] 로그인 API 설계
- [x] 비밀번호 해싱 유틸 추가
- [x] JWT 발급 유틸 추가
- [x] JWT 검증 dependency 추가
- [x] 현재 로그인 유저를 가져오는 `get_current_user` dependency 추가
- [x] 보호가 필요한 API에 인증 적용
- [x] 태스크 생성 시 `creator_id`를 프론트 입력값이 아니라 현재 로그인 유저 기준으로 저장
- [x] 프론트 로그인 흐름을 JWT 방식에 맞게 수정
- [x] 프론트 API 요청에 `Authorization: Bearer <token>` 헤더 추가
- [x] 로그아웃 시 토큰 제거

## 3. 기존 코드 정리 TODO

- [x] `frontend/src/pages/Signup.tsx`의 중복 `nav("/login", ...)` 호출 제거
- [ ] PowerShell 출력 인코딩 문제와 실제 파일 인코딩 문제를 구분해서 확인
- [ ] 실행 명령어를 README 또는 별도 문서에 정리
- [ ] `.env.example` 추가 여부 검토

## 4. 프론트엔드 기능 TODO

- [x] 태스크 수정 UI 추가
- [x] 태스크 삭제 UI 추가
- [ ] 태스크 상세 패널 또는 상세 페이지 추가
- [ ] 칸반 드래그 앤 드롭으로 상태 변경
- [ ] 태스크 검색 UX 개선
- [ ] My tasks 필터 기준을 JWT 현재 유저 기준으로 변경
- [ ] 프로젝트 생성 UI 추가
- [ ] 프로젝트 수정 UI 추가
- [ ] 프로젝트 삭제 UI 추가
- [ ] Inbox 실제 화면 구현
- [ ] Reporting 실제 화면 구현
- [ ] 로딩/에러/빈 상태 UI 정리

## 5. 백엔드 기능 TODO

- [ ] 프로젝트 생성 API 추가
- [ ] 프로젝트 수정 API 추가
- [ ] 프로젝트 삭제 API 추가
- [ ] 스페이스 CRUD API 추가
- [ ] 멤버 초대 API 추가
- [ ] `SpaceMember`, `ProjectMember` 기반 권한 검사 추가
- [ ] 태스크 순서 변경 API 추가
- [ ] 태스크 상태 변경 전용 API 검토
- [ ] 하위 태스크 API 추가
- [ ] 진행률 `progress_rate` API 추가
- [ ] 마감일 `due`를 문자열이 아니라 날짜 타입으로 정리할지 검토

## 6. 테스트/검증 TODO

- [x] 백엔드 API 수동 테스트
- [x] 백엔드 인증 성공 케이스 함수 단위 검증
- [ ] 백엔드 인증 실패 케이스 테스트
- [ ] 프론트 로그인/회원가입 흐름 테스트
- [ ] 프론트 태스크 생성/수정/삭제 흐름 테스트
- [x] `npm run build` 확인
- [ ] 백엔드 테스트 도구 도입 여부 검토

## 7. 추천 진행 순서

1. JWT 인증 방향 결정
2. `Signup.tsx` 중복 이동 코드 정리
3. 백엔드 자체 JWT 인증 구현
4. 프론트 로그인/회원가입을 JWT 방식으로 연결
5. 기존 태스크 API를 인증 기반으로 수정
6. 태스크 수정/삭제 UI 추가
7. 프로젝트 CRUD 추가
8. 스페이스/멤버/권한 기능 확장
